<div class="people-list">
  {{#each @people as |p|}}
    <LinkTo @route="index.show"  @model={{p.id}} class="h-12 flex items-center border-white border-b cursor-pointer no-underline">
    {{p.name}} 
  </LinkTo>
  {{/each}}
</div>
